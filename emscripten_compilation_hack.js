mergeInto(LibraryManager.library, {
    set_pixel: function(idx, col) {
      // This is a stub. The actual implementation will be provided by the host environment.
    },
    size: function() {
      // This is a stub.
      return 0;
    },
    get_pixel: function(idx) {
      // This is a stub.
      return 0;
    }
    // time: function() {
    //   // This is a stub.
    //   return 0;
    // }
  });